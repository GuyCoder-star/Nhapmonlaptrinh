/* nhập vào số nguyên dương n.Kiểm tra xem n có phải số đối xứng  hay không? */

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

void input();
void output();

int n;
bool check = false;

int main() {
    input();
    
    output()
}
void input() {
    printf("nhap so nguyen duong n: "); scanf("%d", &n);
}


void output() {
    if (check ==  true) {
        printf("%d la so doi xung");
    }
    else {
        printf("%d khong la so doi xung");
    }
}